<div class="container">
  <div class="row">
    <div class="col-md-4">
      <%= form_tag '/charges/create' do %>
        <article>
          <% if flash[:error].present? %>
            <div id="error_explanation">
              <p><%= flash[:error] %></p>
            </div>
          <% end %>
          <label class="amount">
            <span>Amount: $5.00</span>
          </label>
          <input type="hidden" name="amount" value="500">
          <label>
            <span>Phone</span>
          </label>
          <input type="text" name="phone">

         <div class="radio">
            <label><input type="radio" name="plans">Option 1</label>
         </div>
         <div class="radio">
            <label><input type="radio" name="plans">Option 2</label>
         </div>
         <div class="radio disabled">
            <label><input type="radio" name="plans">Option 3</label>
         </div>

        </article>

        <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
                data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
                data-description="A month's subscription"
                data-amount="500"
                data-locale="auto"></script>
      <% end %>
    </div>

    <div class="col-md-4">
      <%= form_tag '/charges/create' do %>
        <article>
          <% if flash[:error].present? %>
            <div id="error_explanation">
              <p><%= flash[:error] %></p>
            </div>
          <% end %>
          <label class="amount">
            <span>Amount: $10.00</span>
          </label>
          <input type="hidden" name="amount" value="1000">
          <label>
            <span></span>
          </label>
        </article>

        <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
                data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
                data-description="A month's subscription"
                data-amount="1000"
                data-locale="auto"></script>
      <% end %>
    </div>

    <div class="col-md-4">
      <%= form_tag '/charges/create' do %>
        <article>
          <% if flash[:error].present? %>
            <div id="error_explanation">
              <p><%= flash[:error] %></p>
            </div>
          <% end %>
          <label class="amount">
            <span>Amount: $15.00</span>
          </label>
          <input type="hidden" name="amount" value="1500">
        </article>

        <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
                data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
                data-description="A month's subscription"
                data-amount="1500"
                data-locale="auto"></script>
      <% end %>
    </div>




  </div>
</div>